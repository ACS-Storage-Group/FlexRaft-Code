  
ADD_DEFINITIONS( 
	${RCF_DEFINES} 
	-DRCF_BUILD_DLL )

INCLUDE_DIRECTORIES( ${RCF_INCLUDES} )

ADD_LIBRARY(
    RcfDll
    SHARED 
    ${RCF_ROOT}/src/RCF/RCF.cpp)

TARGET_LINK_LIBRARIES(RcfDll ${RCF_LIBS})
